/**
 * @module
 * wrapping-grid, .wrapping-grid
 * Displays children as a grid that wraps to the next line when it runs out of space.
 * --min-column-width=[LENGTH]
 * sizing=min|max
 * axis=horizontal|vertical
 */
import "./WrappingGrid.css";